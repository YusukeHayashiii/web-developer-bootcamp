<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>タスク 一覧</title>
</head>

<body>
    <h1>タスク一覧</h1>
    <ul>
        <% todoItems.forEach((item)=> { %>
            <li><a href="/todos/<%= item.id %>">
                    <%= item.text %>
                </a></li>
            <% }); %>
    </ul>
    <a href="/todos/new">追加する</a>
</body>

</html>